/* styles.css */

/* Style pour le corps avec un dégradé de rouge et noir */
body {
    font-family: 'Arial', sans-serif;
    margin: 20px;
    background: linear-gradient(to bottom, #b22222, #000000); /* Dégradé de rouge foncé à noir */
    color: #f8f8f8; /* Couleur du texte clair pour contraster avec le fond */
}

/* Style pour les tableaux */
table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 20px; /* Espacement entre les sections */
}
table, th, td {
    border: 1px solid #ffffff; /* Bordure blanche */
}

/* Style pour les cellules du tableau */
td {
    padding: 10px; /* Espacement interne pour les cellules */
}

/* Style pour la cellule contenant l'image */
.photo {
    text-align: right;
    vertical-align: top;
}
.photo img {
    width: 150px; /* Ajuste la taille  */
    height: auto;    
}

/* Style pour les titres */
h2 {
    color: #ffffff; /* Couleur des titres en blanc */
    border-bottom: 2px solid #ffffff; /* Bordure blanche plus épaisse */
    padding-bottom: 5px;
    margin-top: 20px; /* Marge supérieure pour espacer les sections */
}

/* Style pour les listes */
ul {
    list-style-type: disc;
    padding-left: 20px;
    margin: 10px 0; /* Marges autour des listes */
}

/* Ajout d'un style pour les liens */
a {
    color: #f8f8f8; /* Couleur des liens en clair */
    text-decoration: none; /* Supprime le soulignement */
}
a:hover {
    text-decoration: underline; /* Soulignement au survol */
}

